# what flags you want to pass to the C compiler & linker
AM_CFLAGS = --pedantic -Wall -std=c99 -O2 -I../include
AM_LDFLAGS = -lc

lib_LIBRARIES = libmanchicken.a
libmanchicken_a_SOURCES = mutable_string.c
